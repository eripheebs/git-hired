describe('gitHubDataController', function() {
  beforeEach(module('gitHired'));

  var ctrl, GitHubDataService;
  beforeEach(inject(function($controller) {
    ctrl = $controller('gitHubDataController');
  }));


});
